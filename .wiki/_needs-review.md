# Needs review

> Items below were generated automatically and need human validation.
> Answer or validate each item, then remove it from this list.

## Open questions

- **Base**: [TODO] Handle multiple backends with different feature flags.
- **Django**: [HACK] datetime instances cannot be assigned new attributes. Define a subclass
- **Django**: [HACK] the convert_to_local_time flag will prevent
- **Django**: [FIXME] Allow to override this.
- **Django**: [TODO] Modify if/when that internal API is refactored
- **Django**: [FIXME] One day we'll should do this, but not in 1.0.
- **Django**: [TODO] Drop ImportError and KeyError when dropping support for PY312.
- **Django**: [TODO] this should be handled by some parameter to the ChangeList.
- **Django**: [TODO] when dropping support for GEOS 3.13 the z argument can be
- **Django**: [TODO] Adapt timezone information. See:
- **Django**: [TODO] Fix Envelope() for Point geometries.
- **Django**: [TODO] Fix significant figures.
- **Django**: [TODO] Autodetection of multigeometry types (see #7218).
- **Django**: [TODO] Support more than one geometry field / model. However, this
- **Django**: [TODO] It is expensive to retrieve a model for every record --
- **Django**: [TODO] allow deleting individual features (#8972)
- **Django**: [TODO] Add support for `diminfo` column (type MDSYS.SDO_DIM_ARRAY).
- **Django**: [TODO] Is this really the same as ST_Intersects()?
- **Django**: [TODO] Research way to find a more specific geometry field type
- **Django**: [TODO] Support 'M' extension.
- **Django**: [NOTE] If the field is a geometry field, the table is just recreated,
- **Django**: [FIXME] this currently assumes that upload handlers store the file as
- **Django**: [NOTE] There is no need to remap parent dependencies as we can
- **Django**: [TODO] investigate if old relational fields must be reloaded or if
- **Django**: [TODO] colorize this SQL code with style.SQL_KEYWORD(), etc.
- **Django**: [NOTE] We can't modify a dictionary's contents while looping
- **Django**: [FIXME] Rename possibly_multivalued to multivalued and fix detection
- **Django**: [TODO] Handle multiple backends with different feature flags.
- **Django**: [TODO] Handle multiple backends with different feature flags.
- **Django**: [TODO] It might be possible to trim more joins from the start of
- **Expressions**: [FIXME] Rename possibly_multivalued to multivalued and fix detection
- **Options**: [NOTE] We can't modify a dictionary's contents while looping

## Unresolved contradictions

_None detected._
