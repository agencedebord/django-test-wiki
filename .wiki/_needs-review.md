# Needs review

> Items below were generated automatically and need human validation.
> Answer or validate each item, then remove it from this list.

## Open questions

- **Base**: [TODO] Handle multiple backends with different feature flags.
- **Contrib**: [TODO] Drop ImportError and KeyError when dropping support for PY312.
- **Contrib**: [TODO] this should be handled by some parameter to the ChangeList.
- **Contrib**: [TODO] when dropping support for GEOS 3.13 the z argument can be
- **Contrib**: [TODO] Adapt timezone information. See:
- **Contrib**: [TODO] Fix Envelope() for Point geometries.
- **Contrib**: [TODO] Fix significant figures.
- **Contrib**: [TODO] Autodetection of multigeometry types (see #7218).
- **Contrib**: [TODO] Support more than one geometry field / model. However, this
- **Contrib**: [TODO] It is expensive to retrieve a model for every record --
- **Contrib**: [TODO] allow deleting individual features (#8972)
- **Contrib**: [TODO] Add support for `diminfo` column (type MDSYS.SDO_DIM_ARRAY).
- **Contrib**: [TODO] Is this really the same as ST_Intersects()?
- **Contrib**: [TODO] Research way to find a more specific geometry field type
- **Contrib**: [TODO] Support 'M' extension.
- **Contrib**: [NOTE] If the field is a geometry field, the table is just recreated,
- **Core**: [FIXME] Allow to override this.
- **Db**: [NOTE] There is no need to remap parent dependencies as we can
- **Db**: [TODO] investigate if old relational fields must be reloaded or if
- **Db**: [TODO] colorize this SQL code with style.SQL_KEYWORD(), etc.
- **Db**: [NOTE] We can't modify a dictionary's contents while looping
- **Db**: [FIXME] Rename possibly_multivalued to multivalued and fix detection
- **Db**: [TODO] Handle multiple backends with different feature flags.
- **Db**: [TODO] Handle multiple backends with different feature flags.
- **Db**: [TODO] It might be possible to trim more joins from the start of
- **Django**: [TODO] Modify if/when that internal API is refactored
- **Expressions**: [FIXME] Rename possibly_multivalued to multivalued and fix detection
- **Http**: [FIXME] this currently assumes that upload handlers store the file as
- **Options**: [NOTE] We can't modify a dictionary's contents while looping
- **Templatetags**: [HACK] datetime instances cannot be assigned new attributes. Define a subclass
- **Templatetags**: [HACK] the convert_to_local_time flag will prevent
- **Utils**: [FIXME] One day we'll should do this, but not in 1.0.

## Unresolved contradictions

_None detected._
